# Render Blueprint — https://render.com/docs/blueprint-spec
# If you created the service manually, set in Dashboard → Settings → Build & Deploy:
#   Root Directory: (leave empty)
#   Dockerfile Path: Dockerfile

services:
  - type: web
    name: dppflash-backend
    runtime: docker
    plan: starter
    region: frankfurt
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
      - key: NEXT_PUBLIC_DPP_URL
        sync: false
      - key: NEXT_PUBLIC_SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: SUPPLIER_OUTREACH_SECRET
        sync: false
      - key: SUPPLIER_OUTREACH_ENABLED
        value: "false"
      - key: SUPPLIER_OUTREACH_FROM
        sync: false
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USER
        sync: false
      - key: SMTP_PASSWORD
        sync: false
